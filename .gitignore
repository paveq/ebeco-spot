# Built binaries
/ebeco-spot
/bin/

# Local config (may contain device ids) and runtime state
/config.toml
/baseline.json
/baseline.json.tmp

# 1Password env file (secret references; .env.example is committed)
/.env
